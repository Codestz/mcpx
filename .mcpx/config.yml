# Generated by mcpx init from .mcp.json
# See: mcpx --help

servers:
    serena:
        command: serena
        args:
            - start-mcp-server
            - --context=claude-code
            - --project-from-cwd
        transport: stdio
        daemon: true
        startup_timeout: 30s
